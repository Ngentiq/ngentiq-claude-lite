# {Category} Standards

> Generated by /standards on {date}
> Mode: {brownfield|greenfield}
> Source: {Analysis of N files (sampled from M total) across {detected languages} | Best practices for {detected tech stack}}
> Re-run `/standards {category}` to update.

<!-- TEMPLATE INSTRUCTIONS:
     This is a STRUCTURAL template defining output format, not content.
     Sections and subsections are determined by analysis -- they vary by language and framework.
     Replace all {placeholder} values with actual findings.
     Wrap each generated section in generation markers for update-mode tracking.
     The examples below show FORMAT ONLY -- do not copy them as prescriptive conventions.
-->

<!-- generated:{category}:{section_slug} -->
## {Section Name}

<!-- Repeat this subsection block for each discovered/generated convention -->

### {Subsection Name}
- **Convention**: {description of the pattern}
- **Consistency**: {Established|Majority|Observation|Deprecated|Established (template)|Under Review}
- **Rationale**: {why this convention exists -- brownfield: leave empty unless obvious | greenfield: best-practice reasoning}
- **Scope**: {glob pattern, e.g., src/backend/**/*.cs -- omit line entirely if project-wide}
- **Enforced by**: {linter/tool name and rule -- omit line entirely if not tool-enforced}
- **Examples**: {file:line references (brownfield) | illustrative code snippet (greenfield)}
<!-- /generated:{category}:{section_slug} -->

<!-- Repeat the generated section block above for each section the analysis produces.
     Section names come from the analysis, not this template.
     FORMAT EXAMPLES (not prescriptive conventions):

     <!-- generated:coding:naming -->
     ## Naming

     ### Function Names
     - **Convention**: camelCase for all exported functions
     - **Consistency**: Established (94% -- 47 of 50 files with function definitions)
     - **Rationale**:
     - **Enforced by**: ESLint (camelcase rule)
     - **Examples**: src/api/handler.ts:12, src/core/parser.ts:34

     ### Constants
     - **Convention**: UPPER_SNAKE_CASE for module-level constants
     - **Consistency**: Majority (78% -- 39 of 50 files)
     - **Rationale**:
     - **Examples**: src/config/defaults.ts:5, src/core/limits.ts:1
     <!-- /generated:coding:naming -->

     <!-- generated:coding:error-handling -->
     ## Error Handling

     ### Error Propagation
     - **Convention**: Return Result types from service layer, throw at controller boundary
     - **Consistency**: Established (template)
     - **Rationale**: Separates recoverable errors from HTTP concerns
     - **Scope**: src/services/**/*.ts
     - **Examples**:
       ```typescript
       function getUser(id: string): Result<User, NotFoundError> { ... }
       ```
     <!-- /generated:coding:error-handling -->
-->

## Observations Requiring Decision

<!-- Items below 60% consistency (brownfield) or marked {CUSTOMIZE} (greenfield).
     These are NOT enforced by /standards-check until a decision is recorded.
     Each observation uses the template below. -->

### {Pattern Name}
- **Observation**: {what was found, e.g., ~50% camelCase, ~50% snake_case}
- **Decision**: {pending -- team to decide}
- **Decided by**: {name}
- **Date**: {date}
- **Rationale**: {why the decision was made}

<!-- FORMAT EXAMPLE (not a prescriptive convention):

     ### String Quoting
     - **Observation**: ~55% single quotes, ~45% double quotes across 50 files
     - **Decision**: pending -- team to decide
     - **Decided by**:
     - **Date**:
     - **Rationale**:
-->

## Suppressed Conventions

<!-- Patterns detected but explicitly NOT project standards.
     Listed here to prevent re-addition on regeneration.
     Remove an entry to allow the pattern to reappear in future analysis. -->

- `{pattern description}` -- {reason for suppression}

## Inter-Standard Conflicts

<!-- CONDITIONAL SECTION: Only include this section if the cross-file
     consistency check (Step 6.5) found contradictions between this file
     and another standards file. If no conflicts exist, OMIT this entire
     section (heading and table) from the generated output. -->

| This File Says | Conflicts With | That File Says |
|---------------|----------------|----------------|
| {convention from this file} | {other-standards.md} | {conflicting convention} |

## Change History

<!-- Append a row for each generation or update. Never remove rows. -->

| Date | Action | Details |
|------|--------|---------|
| {date} | Generated | {Mode} analysis of {N} files |
