## Summary

- {What this PR does -- the primary change and its motivation}
- {Secondary change or supporting modification, if any}
- {Additional context: what triggered this work, e.g., bug report, feature request, tech debt}

## Changes by Area

### {Area 1} (e.g., "Authentication", "API Layer", "Database")

- `{file path}` -- {what changed and why}
- `{file path}` -- {what changed and why}

### {Area 2}

- `{file path}` -- {what changed and why}

## Acceptance Coverage

{Include ONLY if a plan file exists for this work. Otherwise omit this entire section.}

| Criteria | Status | Notes |
|----------|--------|-------|
| {BAC/QAC item from plan} | Addressed | {brief evidence} |
| {BAC/QAC item from plan} | Partial | {what remains} |
| {BAC/QAC item from plan} | Not in scope | {why deferred} |

## Test Plan

- [ ] {Specific test step derived from actual changes, e.g., "Run `npm test` -- 3 new tests for AuthService pass"}
- [ ] {Manual verification step, e.g., "Login with expired token returns 401, not 500"}
- [ ] {Edge case verification, e.g., "Empty input to parseConfig returns default values"}

## Reviewer Notes

**Key areas to review**:
- {File or area with the most significant or complex changes}
- {File or area where bugs are most likely to hide}

**Design decisions**:
- {Non-obvious trade-off or decision made in this PR and why}

**Complexity**:
- {Any particularly dense or subtle logic the reviewer should spend extra time on}

## Deployment Notes

{Include ONLY if applicable. Otherwise omit this entire section.}

- [ ] {Migration required: describe the migration and rollback plan}
- [ ] {Config change: new environment variable or feature flag}
- [ ] {Dependency change: new/upgraded/removed package}
